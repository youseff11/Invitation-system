Django==5.2.5
qrcode==8.0
Pillow==11.0.0

# اختيارية — لخدمة الملفات الستاتيكية في الإنتاج
whitenoise==6.8.2

# اختيارية — حسب قاعدة البيانات اللي هتستخدمها عبر DATABASE_URL
# Postgres:              psycopg[binary]==3.2.3
# MySQL/MariaDB:         mysqlclient==2.2.6
#   (على PythonAnywhere مثبّتة أصلاً — مش محتاج تنصبها)
